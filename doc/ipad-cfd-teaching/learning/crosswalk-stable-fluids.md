# 对照表：本课程（FVM / icoFoam） ↔ Stable Fluids / FLIP 课程

> 学习者同时在学另一门课（Stable Fluids → 3D FLIP，面向实时图形）。
> 两门课在讲同一条物理定律，但走不同的数值路线。这份文件登记**每一条概念的对应关系与判决**，
> 防止两套心智模型混成一锅。
>
> 每读完那边一课，就往「待判决」里加一行，判完移进上表。

---

## 一条原则：越靠近物理越相通，越靠近实现越危险

两门课可以摞成三层看：

```
第一层  物理层：不可压 Navier–Stokes 方程本身
        ├─ 图形和工程用的是【同一条方程】，一个字都不差
        └─ 判决：完全相通，随便借

第二层  结构层：一步怎么拆
        ├─ 我们：PISO = 动量预测 → 压力方程 → 修正速度（修正在最后）
        ├─ 那边：算子分裂 = 加力 → 平流 → 扩散 → 投影（投影在最后）
        └─ 判决：高度相通。两边都是「先让速度自己走一步，再用压力把它掰回无散度」

第三层  格式层：每一项具体怎么离散
        ├─ 我们：迎风、守恒、按面记账
        ├─ 那边：半拉格朗日回溯插值、不守恒、无条件稳定
        └─ 判决：【这里才分家】。分家的原因不是物理不同，是目标不同
                 我们要守恒与精度；那边要实时与绝不爆炸
```

**记法：物理是共享的，做法是各自选的。** 看到一段内容，先问它属于哪一层。

---

## 三问判别法（自己就能用）

拿到那边任何一段内容，依次问：

1. **它在讲物理，还是在讲数值选择？**
   讲物理的（方程各项是什么、压力为什么存在、梯度为什么驱动流动）→ 直接借。
   讲"我们选择这样算"的 → 继续问下面两条。

2. **它守恒吗？**
   不守恒的东西，可以借它的**直觉**，不能借它的**做法**。
   （我们的第一把验收尺子是「封闭系统总质量守恒到机器精度」；那边根本不以此为目标。）

3. **它的近似在什么极限下失效？**
   每个近似都有适用范围。那边 `dt` 很大、网格很粗也算成功（因为没爆炸就行）；
   我们这边 `dt` 一大就不准（因为要的是精度）。同一个做法，验收标准不同。

---

## 已判决（来自那边第 1 课「网格上的流体」）

| # | 概念 | 我们的说法 | 那边的说法 | 层 | 判决 |
|---|---|---|---|---|---|
| 1 | 流体的状态 | 每个盒子存一组场值（`C` / `U` / `p`） | 每格一个速度向量，场就是流体 | 一 | **同一件事**（都是欧拉描述）。措辞可借 |
| 2 | 不可压 | `∇·U = 0`，连续性方程 | 「任何格子流进多少就流出多少」 | 一 | **完全相同**。那边的大白话更好用 |
| 3 | 压力的角色 | 砖块 11：压力就是"恰好让 `∇·U=0` 的那个量" | 「第二条是约束不是演化方程，它的作用是**决定压力**」 | 一 | **同一句话**。★直接借，讲砖块 11 时用★ |
| 4 | 梯度 vs 差值 | 砖块 4：浓度差是落差，**除以距离才是坡度** | 针筒：0.1 m 上压差 2000 Pa ⇒ 坡度 20 000 Pa/m，「推动水的是坡度不是压力本身」 | 一 | **同一数学结构，不同物理量**。★互相印证，讲 `−∇p` 时回指砖块 4★ |
| 5 | 一步怎么拆 | PISO：动量预测 + 压力修正 ×2 | 算子分裂：加力 → 平流 → 扩散 → 投影 | 二 | **结构相通**，连"修正必须放最后"的理由都一样（前面每一步都会把散度带回来）。借结构直觉 |
| 6 | 对流怎么算 | 显式迎风：`C_new = (1−CFL)·C_P + CFL·C_W` | 半拉格朗日：`d_new = (1−f)·d[i] + f·d[i−1]`，`f = frac(CFL)` | 三 | ★**一维常速、`0 < CFL ≤ 1` 时逐字相同**★（数值验算：逐点差 ~1e-17）。分家只在两处：① `CFL > 1` 时迎风的自留系数 `1−CFL` 变负 ⇒ 爆炸，半拉格朗日改为往更远处回溯 ⇒ 照常；② 速度随位置变化时半拉格朗日失去守恒，迎风按面记账仍守恒。（本行 2026-09-23 更正：原判"形似神不同"说重了） |
| 7 | 数值耗散 | 砖块 10B：`D_num ≈ (u·Δx/2)(1−CFL)` | 第 2 课：`ν_num = h²·f(1−f)/(2·dt)` | 三 | ★**代数上是同一个式子**★：把 `dt = CFL·h/u` 代入它的式子即得我们的。数值验算三组 CFL 完全吻合。第 1 课的"权重和为 1 ⇒ 峰值必降"讲清**为什么**掉，这个公式讲清**掉多少**，两个都要 |
| 8 | 稳定 ≠ 准确 | 砖块 9 笔记：隐式无条件稳定不等于更准/更快 | 把 `dt` 拉到 CFL≈4，染料被抹光但数值不爆炸 | 三 | 同一教训。★借他的**演示手法**：砖块 8A/9 应该做个能拉 `Δt` 的交互★ |
| 9 | 粘性 | cavity 的**全部机制**（Re = 1×0.1÷0.01 = 10，粘性主导） | 「水的粘性太小，本课程直接略去」 | 三 | ⛔ **完全相反，绝不能借**。拿掉 `ν∇²U`，cavity 里什么都不会发生 |
| 10 | 网格布置 | 同位网格（`U` 和 `p` 都在格心）+ Rhie-Chow 式面通量 | MAC 交错网格（速度错开半格） | 三 | 两条路解决同一个问题（棋盘格振荡）。知道有两条即可，**别混着实现** |
| 11 | 回溯的近似 | —（我们没有这个步骤） | `x_b = x − u(x)·dt`，误差 ∝ `dt²` × 速度的空间变化率；RK2 可改善 | 三 | 我们这条路上**不存在**对应物。存档备查，不进任何一砖 |

## 已判决（来自那边第 2 课「数值耗散」）

| # | 概念 | 我们的说法 | 那边的说法 | 层 | 判决 |
|---|---|---|---|---|---|
| 12 | 算出"多了哪一项" | 砖块 4 答疑：`(C_L − 2C_P + C_R)/Δx²` 只在 `Δx → 0` 时才**等于**二阶导，有限 `Δx` 时有离散误差 ∝ `Δx²` | 把 `d[i−1]` 做 Taylor 展开，与精确解相减，剩下的那一项就是方法偷加的东西 | 三 | 同一个工具（修正方程分析）。它就是学习者当时问"前提是不是 `Δx → 0`"的**定量答案**：不只知道有误差，还能算出误差长什么样。★可借，讲离散误差时用★ |
| 13 | 锐利处先塌 | 插页 A：「取邻居平均 = 抹平，**高频误差消得快、低频慢**」（讲的是迭代求解器） | 方波边缘在 CFL=0.99 时先塌掉四分之三，平台峰值却纹丝不动 | 一 | **同一个数学事实**：加权平均是低通滤波，越尖锐（高频）的东西被削得越快。在求解器里它是好事（误差快速消失），在平流里它是坏事（界面被抹糊）。★互相印证★ |
| 14 | 真粘性要不要加 | cavity 的对流项用 `Gauss linear`（中心差分，**没有**迎风的数值扩散；已在本仓库 `tutorials/legacy/incompressible/icoFoam/cavity/cavity/system/fvSchemes` 核实）——目标是让数值粘性**远小于**真粘性 | 「数值粘性比水的真粘性大几个数量级 ⇒ 再加真粘性没有意义」 | 三 | ⛔ **结论相反**。图形里数值粘性压倒真粘性，所以省掉真粘性；工程里必须反过来，否则算出来的是方法的粘性而不是流体的粘性。绝不能借这个结论 |
| 15 | 测试当规格说明 | 验收尺子（守恒 / 有界 / 对称 / 解析解） | `tests/advect1d.test.ts` 先写好 8 条断言，红 → 绿 | — | 方法可借：**把验收尺子写成测试**，写 Swift 代码前先让它们红着。语言无关 |

---

## 待判决

（读完那边新一课后，先自己填「我觉得它和砖块 X 相通，因为……」，再一起判。）

| 那边第几课 | 内容 | 我的假设 | 判决 |
|---|---|---|---|
| | | | |

---

## 怎么往里加

1. 一次贴**一课**，不要一次贴完。
2. 贴的时候**先说你的假设**：「我觉得这一段和砖块 X 是同一件事，因为……」。
3. 我按上面的三问判卷，然后往表里加行。

第 2 步不能省。自己先提假设再被纠正，比直接看答案记得牢得多——这正是两门课并行学习真正的价值所在。
