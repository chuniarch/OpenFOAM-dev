# 插页 A 通过；开始在代码层面追问「对应关系」与「代价」

插页 A 三道练习全对，学习者自评"比较简单"。同时主动提出两个超出课文的问题：
① 手算的高斯-塞德尔和 `symGaussSeidelSmoother.C` 的核心循环逐行怎么对应；
② 每遍求"最大改动"要遍历所有网格，几百万网格时是否太耗时（时间复杂度）。

**Evidence**：练习全对（自述，2026-09-26）；两个问题本身。

**Implications**：
1. 学习者已经在用"读源码"的眼光看课文，可以更早、更多地把课文和 OpenFOAM 真代码逐行对照——这正是 MISSION 里
   "拿到 icoFoam.C 能逐行说出每个算子对应自己写过的哪一块"的方向。
2. 对算法代价敏感：插页 B（遍数随网格增长）与插页 C（更快的解法）会有很强的动机，不必再额外铺垫"为什么要关心速度"。
3. 练习难度要上调（已写入 NOTES 第 13 条）。
