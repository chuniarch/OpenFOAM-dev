# 能用 w = D·A/d 算任意一道墙的权重，包括两侧格子不等宽

学习者第 4 课三道练习全对：`d` 减半则 `w` 翻倍；不等宽网格上 `d = Δx_P/2 + Δx_N/2`（0.10 m 与 0.02 m 两格 ⇒ 0.06 m）；
错拿一侧格宽当 `d` 会让 `w` 偏小。

**Evidence**：第 4 课页面内练习，三题一次答对（2026-09-24 自述）。

**Implications**：`w` 与"`d` 是中心到中心的距离"可以当共识引用。砖块 5 的 `2k` 直接建立在这条上
——边界墙的 `d` 就是半格。
