# 稳态 = 净流入为零 = 邻居的加权平均

学习者答对砖块 2 的加权版本：`w_L = 2·w_R`、`C_L = 10`、`C_R = 4` 时算出 `C_P = 8`，
说明他能正确使用权重做加权平均，而不只是背下"取平均"。

**Evidence**：给出了 `(2*C_L + C_R)/3 = 8` 的完整算式。

**Implications**：`C_P = Σ(w·C_n)/Σw` 这条骨架可以当作共识直接引用，不必重讲。
但他当时未能自己说出"为什么不是 7"，串联配平律（`w·ΔC` 相等）是我补的 —— 该点尚未经过检验。
